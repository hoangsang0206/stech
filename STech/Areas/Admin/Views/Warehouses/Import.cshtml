@{
    ViewData["Title"] = "Nhập kho - STech Admin";
    ViewData["PageTitle"] = "Nhập kho";
}

@model Tuple<Employee, IEnumerable<Warehouse>, IEnumerable<Supplier>>

@{
    Employee employee = Model.Item1;
    IEnumerable<Warehouse> warehouses = Model.Item2;
    IEnumerable<Supplier> suppliers = Model.Item3;
}

<section class="page-wrapper">
    
    <div class="mb-3">
        <button class="page-btn btn-blue">
            <i class="fa-solid fa-list-ul"></i>
            <span>Danh sách phiếu nhập</span>
        </button>
    </div>
    
    <div class="page-content-box">
        <div class="page-content-header">
            Thông phiếu nhập
        </div>

        <div class="row m-0 p-0 import-infomation">
            <div class="col-12 col-md-6 m-0">
                <div class="page-input-box">
                    <label>Kho nhập</label>
                    <div class="page-input">
                        <select id="warehouse_id">
                            <option selected>Chọn kho nhập</option>
                            @foreach (Warehouse warehouse in warehouses)
                            {
                                <option value="@warehouse.WarehouseId">@warehouse.WarehouseName</option>
                            }
                        </select>
                    </div>
                </div>

                <div class="page-input-box">
                    <label>Nhà cung cấp</label>
                    <div class="page-input">
                        <select id="supplier_id">
                            <option selected>Chọn hãng sản xuất</option>
                            @foreach (Supplier supplier in suppliers)
                            {
                                <option value="@supplier.SupplierId">@supplier.SupplierName</option>
                            }
                        </select>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-6 m-0">
                <div class="page-input-box">
                    <label>Nhân viên nhập</label>
                    <div class="page-input">
                        <input type="text" placeholder="Tên nhân viên" value="@employee?.EmployeeName" disabled="disabled"/>
                    </div>
                </div>

                <div class="page-input-box">
                    <label>Ngày tạo phiếu</label>
                    <div class="page-input">
                        <input type="text" placeholder="Ngày tạo phiếu" value="@DateTime.Now.ToString("MM/dd/yyyy")" disabled="disabled"/>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="page-content-box mt-3">
        <div class="page-content-header">
            Sẩn phẩm nhập
        </div>

        <div>

        </div>
    </div>

    <div class="mt-3">
        <div class="d-flex justify-content-end">
            <button class="form-submit-btn page-btn-sm btn-green">Tạo phiếu nhập</button>
        </div>
    </div>

</section>